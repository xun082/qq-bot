node_modules
dist
# WARNING: Including `.env` in the build context will bake secrets into the image.
# Keep ignored by default. Remove these lines only if you intentionally want that.
# .env
# .env.*
.git
.gitignore
*.md
.DS_Store
.cursor
*.log
